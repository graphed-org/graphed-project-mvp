# graphed → coffea NanoEvents, fastjet, conda-forge — worklog

## TOC
- [0. Owner directives + authorizations (2026-09-18)](#0-owner-directives--authorizations-2026-09-18)
- [1. Scouting (2026-09-18)](#1-scouting-2026-09-18)
- [2. Understand workflow](#2-understand-workflow)
- [3. Plan](#3-plan)
- [4. Owner directives (later): fastjet bug PR; MIT LICENSE](#4-owner-directives-2026-09-18-later-fastjet-bug-as-its-own-pr-mit-license-graphed-org-members)
- [5. Plan review r1 → r4](#5-plan-review-r1--plan-r3-2026-09-18)
- [6. Execution (2026-09-18 →)](#6-execution-2026-09-18-)

## 0. Owner directives + authorizations (2026-09-18)
- Integrate graphed into scikit-hep/coffea: hook into NanoEvents in parallel to the dask backend.
- Integrate graphed into scikit-hep/fastjet "similar to how dask is".
- Start conda-forge feedstocks for graphed, graphed-executors, graphed-histogram.
- Authorized: PRs to graphed for features coffea integration needs; REQUESTS for new releases (owner cuts them).
- Authorized: another PR to uproot, or altering scikit-hep/uproot5#1720.

## 1. Scouting (2026-09-18)
- hist#721 MERGED 2026-09-17T18:25Z (`gh pr view 721 --repo scikit-hep/hist`); hist 2.11.0 on PyPI has no `hist.graphed` yet.
- graphed-org has no coffea or fastjet fork (`gh repo list graphed-org`); precedent naming: `uproot5-graphed-mvp`, `hist-graphed-mvp`.
- conda-forge: no `{graphed,graphed-executors,graphed-histogram}-feedstock` (404; control `awkward-feedstock` found); no staged-recipes PR mentions graphed.
- PyPI 0.0.2: graphed = sdist (5.9 MB) + cp311/cp314 wheels, deps `executing>=2.0`, `cloudpickle`, py>=3.11, MIT; executors = pure (`graphed>=0.0.2`); histogram = pure (`boost-histogram>=1.4`, `graphed>=0.0.2`, `numpy>=1.24`).
- coffea main c917005 (2026.9.0): dask mentions per file — nanoevents/methods/nanoaod.py 142, analysis_tools.py 120, methods/fcc.py 86, util.py 52, dataset_tools/preprocess.py 47, factory.py 37, … `_allowed_modes = {"eager","virtual","dask"}`; dask arm = `uproot.dask(..., open_files=False, form_mapping…)`.
- `uproot.graphed` (fork c609298) has NO `form_mapping` parameter → first expected gap.
- fastjet main 4ecbf5c: dask arm in `src/fastjet/_pyjet.py` + dispatch in `__init__.py`; `tests/test_008-dask.py` (275 lines).
- `~/vibe-coding/coffea-workdir` is the owner's coffea PR-review clone — not used.
- Probe venv: `<session scratchpad>/scout/venv` (py3.13; coffea editable, graphed stack 0.0.2, fastjet 3.5.1.4 wheel, fork uproot → version string `0.1.dev1203+gc60929891`).

## 2. Understand workflow
- `wf_6e12b3bc-5c1`: six opus/xhigh readers — nano-factory-seam, nano-behaviors, graphed-seams, fastjet-dask, conda-recipes, coffea-dask-surface; findings under `<scratchpad>/scout/lanes/<label>/findings.md`.
- DONE (6 lanes, 0 errors). Agents could not write findings files (harness refuses subagent report files) → the lead persisted each lane's structured result to `coffea-fastjet-conda-findings/<lane>.md` (TOC: Shape/Seams/Gaps/Works today/Traps/Open decisions/Not established).
- Headline measurements: a hand-built graphed source carrying coffea's NanoAOD schema form already records + executes NanoAOD analyses bit-identical to coffea eager (properties, methods, cross-references; ThreadExecutor and a real process pool with backend by import-ref). All 18 nanoaod `dask_property` arm bodies run verbatim with the graphed array as `dask_array` (22/22 nano_dy, 36/40 pfnano — the 4 are NaN-comparison artefacts). conda: all three v1 recipes build + lint clean on osx-arm64 (graphed as one abi3 build).
- Verified by the lead: NO LICENSE file tracked in graphed (0 matches, control README=1) and none in any of the three 0.0.2 sdists (control pyproject.toml=1) → blocks conda submission on the owner's copyright line.
- Two lanes took different cross-reference designs (attrs self-reference inside one `field` op vs explicit `.graphed` arms through a coffea Array subclass); plan r1 picks explicit edges (§2.3).

## 3. Plan
- `coffea-fastjet-conda-plan.md` r1 written 2026-09-18: units L, C, m58–m60 (graphed), R (0.0.3 request), U (uproot form_mapping/backend, stacked on #1720), F0/F1 (fastjet), A1 (coffea NanoEvents mode="graphed"); later phases named in §6.
- Lead-added after writing: `uproot.graphed` hardcodes `Session(AwkwardBackend(behavior=…))` → U needs a `backend=` passthrough for coffea's `array_type()` subclass.
- Plan review r1: `wf_8f51363c-14a` — lenses plan-facts / plan-design / plan-upstream (opus xhigh), lead adjudicates.

## 4. Owner directives 2026-09-18 (later): fastjet bug as its own PR; MIT LICENSE, "graphed-org members"
- fastjet: bug reproduced by the lead (`full[2:]` → offsets start 9 → clusters event 0/1's particles; `to_packed` control matches `full_jets[2:]`). Root cause `src/_ext.cpp::interfacemulti`: loop bounds from starts/stops but sequential `*pxptr++` reads. Searched `_ext.cpp` for the sequential-read shape: 12 sites, only `interfacemulti` pairs it with starts/stops (the others walk a flat array from 0). `_generalevent` already repacks (`to_ListOffsetArray64(True)`); `_multievent`/`_singleevent` do not.
- Fix = index by `j` in C++ (also covers reordered ListArrays; no copy) instead of the lane's `to_packed` suggestion. Fork `graphed-org/fastjet-graphed-mvp` created, clone `~/vibe-coding/fastjet-graphed-mvp`, branch `fix-nonzero-list-starts`, commit 8f01dc1, **scikit-hep/fastjet#400**.
- Legs (identical env, dask 2025.3.1): released wheel 3.5.1.4 → the 3 new tests FAIL (sliced, reordered, 2 dask partitions); source build of the branch → 94 passed; the 1 remaining failure on both legs is `test_inclusive_from_file` (uproot not installed in the scratch venv).
- Build trap resolved: a local source build works WITHOUT CGAL (brew boost/gmp/mpfr/swig + gnubin make/libtool on PATH; `uv pip install <repo>`), ~minutes. Env trap: dask/distributed 2026.8.0 with dask-awkward 2026.2.1 fails fastjet's own `test_008-dask` (`Cannot fuse tasks with multiple outputs`) — version skew, not fastjet code.
- LICENSE: graphed#31, graphed-executors#16, graphed-histogram#16 (MIT, "Copyright (c) 2026 graphed-org members"). Verified by building: sdist contains LICENSE + `License-File` metadata for all three (0.0.2 sdists: none). Precommit gate `--fast` ok ×3 — needs `m52/.venv/bin` and `~/.cargo/bin` on PATH plus `RUSTUP_TOOLCHAIN=stable-aarch64-apple-darwin` (rustup has no default toolchain on this machine; `PYTHONPATH=graphed-orchestrator/src`).
- conda drafts persisted to `graphed-workdir/conda-recipes/` with the owner's copyright line. Plan → r2.
- CI watcher for the four PRs: background task `bktwii6y8` (2 h, log `<scratchpad>/ci_watch_prs.log`).

## 5. Plan review r1 → plan r3 (2026-09-18)
- r1 (`wf_8f51363c-14a`, raw output `coffea-fastjet-conda-findings/plan-review-r1.md`): 31 load-bearing claims confirmed by probe; 14 design findings (overlapping). Resolved by editing the design:
  - `_replay`-carries-attrs dropped from m58 — it only fails under the REJECTED attrs-in-a-field-op design; under explicit edges projection of `Jet.matched_gen.pt` works on stock 0.0.2 (two reviewers, independent probes).
  - `aggregate_plan(columns=)` override → `PartitionedSource.projected_columns(outputs)` hook (the override put the read-set burden on the end user and had no owner; `necessary_columns` is per-array and DATA-only so a count-only output would under-read).
  - shim routing is now an ordered partition (a)–(f) with a route-walk test; the `Systematic` arms (also `map_partitions`) are refused; physlite dropped from A1; `__graphed_capable__` only where A1 has a parity test.
  - `_ensure_flat` NOT taught the graphed type (would make `PackedSelection.add` a silent no-op; today it raises); `steps_per_file` refused in graphed mode.
  - fastjet Externals keyed by `graphed.execute.external_key` with query name+args as params (payload-only hash would collapse N queries onto one evaluator); `graphed` extra marked py>=3.11; F1 needs F0; dev loop = source build.
  - §3 Needs split into dev (git main / fork branch) vs upstream (released artifacts); U upstream needs R; conda 0.0.3 bump includes the license_file switch; fat LTO added to the draft recipe; §4 registration = `bookkeep.py --set-current` + root-prompt R-section, not the gated plan.
- Intel from the upstream lens: owner has merge rights in coffea + fastjet; uproot merge→release median 38 d; coffea shows no dask-mode deprecation; `coffea.compute` (WIP #1470) is orthogonal; awkward-zipper migration (#1479) argues for keeping the uproot/schema boundary; #1720 review comments so far all addressed.
- r2 delta review: `wf_240ca279-a4b` (delta-facts, delta-design).
- r2 delta (`wf_240ca279-a4b`, raw `coffea-fastjet-conda-findings/plan-review-r2.md`): all 14 r1 findings CLOSED; 11 new design findings on the delta (≈8 distinct) → plan r4:
  - route (f) must be `graphed.Array.__getattr__` unchanged — a bare BoundMethod there breaks the 23 plain `@property` names that work on stock graphed; route test population = fields + descriptors (incl. the 5 `no_dispatch`) + plain properties (AST: 73 descriptor names → b5/c2/d2/e60/f4).
  - `projected_columns` found by `getattr`, NOT a Protocol member: adding a member to the `runtime_checkable` `PartitionedSource` makes every existing source fail `isinstance` (probe: parquet + flat uproot → False) and `aggregate_plan` then sees 0 sources. One helper at three sites: `aggregate.py` + the two write drivers in `awkward/io.py` (`_evaluation_columns*`); `read_columns_by_label` stays syntactic.
  - fastjet arm exposes NO externals mapping: `record_external(op, fn, …)` registers `fn` on the Session and `aggregate_plan` auto-wires by `external_key` (lead verified in `aggregate.py::_external_evaluators`). The fastjet lane's "evaluator does not travel" claim was wrong for `aggregate_plan`.
  - `allow_read_errors_with_report` is a `uproot_options` key, not a `from_root` parameter; `from_parquet(mode="graphed")` would fail OPEN once "graphed" joins `_allowed_modes` → explicit raise; `__graphed_capable__` must be own-body (`vars()`), since ScoutingNanoAODSchema subclasses NanoAODSchema; F1 upstream needs F0 merged.
- r3 (`wf_ce74bbe2-7dd`): one reviewer — r2 closure on r4, then the whole-artifact pass if the delta is clean.

## 6. Execution (2026-09-18 →)
- r3 review clean → plan r4 committed in meta 7b6dba4 (local, unpushed) with `m58-decomposition.md`, root prompt R25.0/R25.1, state `current_milestone=m58`.
- Fork `graphed-org/coffea-graphed-mvp` created (not cloned yet). graphed branch `m58-projection-declarations` off origin/main in `~/vibe-coding/m52/graphed`.
- m58 TEST_AUTHORING: `wf_87b79e98-7ef` (one opus/xhigh test-author, writes only `tests/frozen/awkward/m58/`, no git).
- fastjet#400: 26/27 green; the one red was a quay.io image-pull EOF on `Wheel: 314t, auto64` (infra) → `gh run rerun 35396255697 --failed`.
- Unit C: conda-forge `python_min` is 3.11 globally (live pinning, = graphed's floor → no override); PyPI sdist sha256 ×3 re-checked equal to the recipes; `conda-smithy recipe-lint --conda-forge` ×3 "in fine form" from a staged-recipes clone (`<scratchpad>/conda/staged-recipes`, branch `graphed-stack`, fork remote `lgray/staged-recipes`). Tools: `<scratchpad>/scout/lanes/conda-recipes/cfenv/bin/{rattler-build,conda-smithy}`.
- Unit C submitted: **conda-forge/staged-recipes#34887** (head `lgray:graphed-stack`). Local fat-LTO build of `graphed` on osx-arm64: `✔ all tests passed!` (pip check, abi3audit, import); package 4715569 B vs the strip-only lane build 4738411 B (the setting reached cargo). Review-team ping (`@conda-forge/help-python-c` / `help-rust`) waits on green linux/win/osx CI. Log `<scratchpad>/conda/build-graphed-lto.log`.
- coffea fork cloned: `~/vibe-coding/coffea-graphed-mvp` (origin = graphed-org fork, upstream = scikit-hep/coffea @ c917005e).
- fastjet#400 after the rerun: 28/28 checks pass (`gh pr checks 400 --repo scikit-hep/fastjet`). Merge = owner/upstream call.
- m58 test-author done (11 tests: `test_read_list.py` 5, `test_plan_boundary.py` 3, `test_external_stand_in.py` 3 + `m58_declaration_fixtures.py` + README). Lead TEST_SANITY: two runs `F.FFF.FFF..` identical modulo object addresses; 7 fail on main for their own reason (`source.calls == 0`; H4 `('x','y') != DECLARED`; E1 AxisError on `inputs[0]`; E3 no field `total`); each of the 4 controls dies under a mutation of its fallback (H2 reversed `read_columns`; H2-protocol grown Protocol; H5 reordered by-label; E2 inputs untouched → `{'events': frozenset()}`; the unmutated leg passes). Script `<scratchpad>/m58/mutate_controls.py`.
- Three spec gaps accepted into `m58-decomposition.md`: H3 witness runs on `SequentialRunner` (graphed does not depend on graphed_executors); the varied write builds its own output list (H1 there pins once + session Arrays); E needs `on_fail="pass"` (default policy refuses every External before a stand-in exists → U's hook must project with `on_fail="pass"`).
- Gate bug found + fixed: `precommit.check_integrity` read STAGED-new frozen files as `frozen-modified` (`git diff HEAD --name-only` lists additions) → **graphed-orchestrator#1** (ab4e53a, branch `fix/staged-new-frozen`; new test fails without the fix; the 3 local coverage-test failures are the missing `--extra dev`, 28/28 with it). Until merged: run the gate with a new suite untracked. Frozen commit 049e3c7, tag `freeze-m58` (local).
- Lead-measured for the implementer brief: graphed-histogram records fills with its own `HistogramForm` (no typetracer) → the stand-in rule is "recorded AwkwardForm → its typetracer, else inputs[0]".
- m58 IMPLEMENTING→REVIEW: `wf_d0017919-50b` (opus/high implementer, then opus/xhigh reviewer on `freeze-m58..HEAD`).
- staged-recipes#34887 CI: linter + `build linux_64` / `osx_64` / `win_64` all pass → review ping posted (`@conda-forge/help-rust @conda-forge/help-python`). Next on that PR: reviewer feedback only.
- m58 iter 1 (`wf_d0017919-50b`): implementer stopped with a TEST DISPUTE (correctly — no gate gamed). 10/11 frozen green on a 39-line src diff (`write.py::declared_columns` + 3 sites + `on_external` stand-in); H3 cannot pass: the `DeclaringSource` fixture is the plan's `reader` and retains the output Arrays → `cannot pickle 'builtins.GraphStore'`; behind it `seen` travels through pickle (4 reads vs the 2 asserted). Lead verified all three probe legs (as frozen → TypeError; drop `outputs` → `seen` fails; drop both → passes). Root cause in the process: assertions behind the first failing one never ran at TEST_SANITY.
- Adjudicated CORRECTED. Impl diff parked as `<scratchpad>/m58/m58-impl-iter1.patch`, tree restored to the freeze; test-author recalled (`wf_06efd98f-697`) to amend the fixture AND exercise every behind-the-first-failure leg by simulation. Then: lead TEST_SANITY, gate `--allow-refreeze tests/frozen/awkward/m58`, tag `freeze-m58-2`, re-apply the patch, implementer → reviewer.
- Dispute correction (`wf_06efd98f-697`): fixture `DeclaringSource.__getstate__` (answer + counter ship; `outputs`/`seen` local) + control `test_a_pickled_declaring_source_keeps_its_counter_and_drops_its_witness_state` (dies without `__getstate__`: GraphStore TypeError); every assertion behind a first-failing one run by simulation. Lead re-sanity: on the freeze tree 7 fail (same set) / 5 pass, two runs identical; with the parked impl applied 12/12. Gate `--allow-refreeze tests/frozen/awkward/m58` ok → commit 1c5a922, tag `freeze-m58-2` (local). Patch re-applied.
- m58 iter 2: `wf_58e01b1c-fa8` (implementer finishes extra tests/docs/gates/commit → reviewer on `freeze-m58-2..HEAD`).
- m58 iter 2 (`wf_58e01b1c-fa8`): implementer green → a5b8e45 (12/12 frozen, 6 extra legs each mutation-killed, whole `run-tests.sh` rc=0, gate ok, frozen-suite diff coverage 25/26 arms). Reviewer REJECT, ONE design finding: a declared EMPTY read list is honoured by `aggregate_plan` only — `_WritePart._chunk`/`_VariedWritePart._chunk` read with `self.columns or None`, and `()` is already the write drivers' word for "everything" → a declared `()` reads the whole source; docstring + design.rst claimed otherwise; mutants `if not columns:` survive at both write sites. 17 mutants total: 12 killed, 2 = the finding, 3 equivalent/exit-round. graphed-histogram 214 passed on the branch.
- Lead decision: honour verbatim everywhere — translate the write drivers' own sentinel to None where it is PRODUCED, `_chunk` passes `self.columns` through, fields widen to `tuple|None`. Constraints forwarded: tuple-argument leg at the varied driver; `on_external` → `form.tt if isinstance(form, AwkwardForm) else inputs[0]` (behavior wrap is a measured no-op); str-answer docstring sentence. Dropped: isinstance(PartitionedSource) guard before asking (no real source is hook-bearing and non-partitioned; the own list is equally unused on that path).
- m58 iter 3: `wf_11155bbc-748` (repair → delta review on `a5b8e45..HEAD`).
- m58 iter 3 (`wf_11155bbc-748`): a0668f6 `fix(m58): a declared read list survives to every write worker`; delta reviewer **APPROVE**, 0 design findings, 9/9 delta mutants killed, hook-less control byte-identical vs the freeze tree (file sha256 + read-back, incl. the repo's parquet source), whole `run-tests.sh` rc=0 (68 subtrees), graphed-histogram 214 passed. Exit-round notes (no action): `_DatasetLoader.read_partition` also reads falsy as "everything" but no `()` can reach it; mutation trap = stale `__pycache__` within one second → `PYTHONDONTWRITEBYTECODE=1`.
- **graphed#32** opened (branch `m58-projection-declarations`, tags `freeze-m58`, `freeze-m58-2` pushed). `sphinx -W` not run locally (m52 venv lacks `myst_nb`) → CI docs job is the check. CI watcher: background `b9h6y8dmz` (`watch_ci.py`, graphed a0668f6 + orchestrator ab4e53a, 90 min cap).
- Envs for the fork units: worktree `~/vibe-coding/uproot5-graphed-form-mapping` (branch `graphed-form-mapping` off origin/graphed-mvp 73b479f; the main fork clone's local graphed-mvp is diverged — left alone); `~/vibe-coding/integ/venv` (py3.13: uproot editable from that worktree, coffea editable from `~/vibe-coding/coffea-graphed-mvp`, dask 2025.3.1, graphed stack via `graphed_dev.pth` → `m52/graphed/python` = whatever branch is checked out there). fastjet: branch `graphed-arm` off the fix; `<scratchpad>/fj/venv-src` site-packages `fastjet/*.py` symlinked into the clone (+ dangling `_graphed.py`), graphed via .pth.
- U + F1 TEST AUTHORING: `wf_0325f6c8-9dd` (two opus/xhigh test-authors, upstream style, hidden-leg simulation required). Then lead sanity → implementers → reviewers.
- graphed#32 CI: 32/32 pass on a0668f6 (incl. `docs (sphinx -W)`, `coverage (>=90% combined)`, `ci required`); graphed-orchestrator#1 CI green. m58 = DONE pending the owner's merge.
- Fork-unit envs re-pinned to a DETACHED m58 worktree `~/vibe-coding/integ/graphed-m58` (a0668f6, `.so` copied) so m59 edits in `m52/graphed` cannot disturb U/F1 runs.
- m59 registered: `m59-decomposition.md` (I1–I6 multi-axis keys that leave axis 0 whole / S1–S5 numpy-scalar dtype + exact value as a str param / M1–M4 nested method outputs), root prompt R25.2, `bookkeep --set-current m59`, meta commit 7007525 (local). Lead probes: `ev.x[:, :2]` TypeError today; `bool * np.uint64(1)` → float64 vs eager uint64; python scalars already match. Branch `m59-awkward-idiom-parity` stacked on m58. TEST_AUTHORING: `wf_d64ec670-2b8`.
- U + F1 test authoring (`wf_0325f6c8-9dd`) done; both authors built a STAND-IN implementation as a pytest plugin to run every hidden leg and measured discrimination per property. Lead sanity: U 10 failed / 1 control passed (uproot 92333d4: `tests/test_1720_graphed_form_mapping.py`, `…_mapped_projection.py`, `tests/graphed/mini_nanoaod.py`; the 16 pre-existing test_1720 files 106 passed); F1 18 failed / 2 controls passed (fastjet dad8606: `tests/test_010-graphed.py`; pre-existing 95 passed). Measured on the way: `uproot.graphed` silently DROPS `form_mapping=`/`known_base_form=`/`backend=` into `**options` today; `aggregate_plan` refuses an in-memory (`from_awkward`) source, so in-memory fastjet legs execute via `session.materialize`; `graphed.vary(points=)` takes member ARRAYS.
- Lead decisions on the authors' gaps: keep the `test_1720_` prefix until the stacked PR has a number; `necessary_buffers` on a mapped array answers in TBranch names; `backend=` with `behavior=` → TypeError; `uproot.graphed_write` over a mapped source must work (declared branches only) or refuse loudly naming `to_parquet` — with a test either way; `ClusterSequence` admits a `graphed.Varied`; the pyproject marker is a reviewer item (fastjet's suite has no packaging-metadata test precedent). Carried to A1: heterogeneous file sets (`!loadallowmissing`) are untested under graphed.
- U + F1 IMPLEMENT→REVIEW: `wf_88aee2fe-bd9` (pipeline per unit; U reviewer must prove parity with coffea's REAL `_map_schema_uproot` on `nano_dy.root` — what A1 stands on; F1 reviewer checks signature parity with the dask half, hash stability across processes, the namespace-package trap).
- m59 test authoring (`wf_d64ec670-2b8`): `tests/frozen/awkward/m59/` (48 tests) + `tests/frozen/numpy/m59/` (11). Unimplemented tree: awkward 31 fail / 17 pass, numpy 4 fail / 7 pass; lead re-ran twice per dir → identical FAILED lists; 7 control mutations reported, each kills its control; hidden legs simulated (146 S-legs through `_ops.apply` — which caught a fixture bug: numpy forbids bool subtract). Measured for the implementer: the numpy idiom ALREADY has a tuple-key `subscript` op (`graphed/numpy/array.py::_encode_subscript`, param `spec`) → reuse, one encoding; the Rust store's int ParamValue is i64 (`2**63` raises) so wide uint64 needs a non-int encoding; `GraphedTypeError` is not a `TypeError` subclass → key validation stays in `Array.__getitem__`; a numpy scalar on the LEFT of a comparison raises in eager awkward itself (S1 wording narrowed in the decomposition).
- Gate ok (suite untracked) → commit db2094c, tag `freeze-m59` (local). m59 IMPLEMENT→REVIEW: `wf_03b3bdc3-7a5`.
- U + F1 iter 1 (`wf_88aee2fe-bd9`): both implementers green (uproot e8cfe39 +329/-56, `_graphed.py` 100 % line+branch; fastjet 950f952, 261 src lines, 34 explicit forwarders), both reviewers REJECT.
  - U reviewer CONFIRMED the A1 premise: coffea's real `_map_schema_uproot(NanoAODSchema)` on `nano_dy.root` → form == dask meta, count-only + sum bit-for-bit, read list `['Muon_pt','nJet','nMuon']`, `*Idx` branches found, real process pool ok (source pickles 31 kB). Note for A1: `*IdxG` global indices at steps_per_file>1 differ from eager IDENTICALLY in coffea's own dask arm (partition-local by construction).
  - U findings: (1) declared `()` discarded by `read_partition`'s `if columns` → 1499 branches read for `gak.num(events, axis=0)` — the m58 falsy-means-everything shape again, at the consumer this time; (2) `known_base_form=` renames the source to "events"; (3) `<root>` guard unpinned. Constraints: M6 behavior line (kill or delete), executors via open options, `prepare_docstrings.py` un-hide, floor `>=0.0.3`.
  - F1 findings: (1) SIGSEGV at RECORD time on a flat single-event input (10 two-array queries; length-zero recipe vs `_singleevent.py`) → lead decision: the arm REFUSES flat input (first axis = partition axis; plan decision 5 amended; test-author amends the acceptance test); (2) cross-session second operand silently evaluated against the other session's node — root cause `Session.record_external` does not refuse foreign inputs (record_op does) → added to m60's scope in the plan, F1's test lands after; (3) jetdef↔node identity untested (constant-hash mutant survives, R=0.6 answers with R=0.4's jets); (4) namespace-package guard unpinned; (5) graphed tests inert in CI (`test` extra lacks graphed).
- Iter 2: `wf_df37cc9e-206` (U repair → delta review ‖ F1 amend → repair → delta review).
- m59 iter 1 (`wf_03b3bdc3-7a5`): implementer green — 1882fa7 (keys: the numpy idiom's `subscript`/`spec` codec MOVED to `array.py::encode_subscript/decode_subscript` and widened; `NumpyArray.__getitem__` deleted) + f5470b8 (scalar `dtype` param, wide ints as text; nested `method_outputs`). Frozen-vs-frozen conflict resolved without touching either: frontend/m13 pins `a[:, 1]` REFUSED on the base proxy (ToyBackend) → a backend DECLARES `subscript_keys = True` (getattr, like `array_type`). Premise corrected by measurement: the Rust store ACCEPTS `2**63` and silently widens to f64 (no exception) → text encoding buys exactness. Frozen-suite diff coverage 97.8 %; `run-tests.sh` rc=0 (70 subtrees).
- Reviewer REJECT, 4 findings (21 mutants: 19 killed, 1 equivalent, 1 real survivor): (1) a leading `...` admits axis-0-consuming keys as fusible ops (`root[..., 0]` on `4 * float64`: 1 vs 2 partitions differ) → lead decision: a backend typing rule (Form is opaque to the frontend), decomposition I1b added; (2) `_refuse_axis0`'s `rstrip(':')` tells `a[1:, 0]` to write `a[1][:, ...]`; (3) S2 wide-int branch unkilled (materialize never reads the store) → durable-path leg with non-power-of-two values; (4) duplicated scalar decoder → NOT hoisted (independent backends, numpy-free frontend); both readers reduced to one line by deleting the equivalent `int(...)` arm. Not doing: numpy-int key members, complex operands, zero-step guard (eager awkward hangs identically).
- m59 iter 2: `wf_b2f640e8-8b6`.
- U + F1 iter 2 (`wf_df37cc9e-206`): every iter-1 finding closed; both delta reviewers REJECT on the repairs' own new surface.
  - U (c7ccc2e), 3 findings: (1) options-at-open left one of six open sites — `_graphed_write.py::_write_partition` opens bare, so `graphed_write` drops executors while the new docstring says every read carries them; the `read_partition` fix itself is unpinned (M3 survives); (2) `_name_from_object_path` sub-behaviours unguarded (TDirectory strip, `;cycle` strip, fallback); (3) un-hiding `uproot._graphed` publishes 4 dead `:doc:` roles (3 unqualified, `uproot._graphed.compute` does not exist) → docstring-only, handed as a constraint.
  - F1 (c5fcb0a), 1 design finding: `all(`→`any(` in `_holds_one_event_per_entry` survives and the mixed-ndim Varied it admits exits 139. The unsatisfiable `graphed>=0.0.3` floor is sequencing, not a finding: both branches go upstream only after the release (plan §3).
  - Shape of both rounds: a repair ships a branch or a claim with no killing test, and repairs the coordinate the finding named rather than the enumerated class. Dispatch constraint from here: enumerate the class with the reviewer's instrument first; every new/changed branch reports the mutant its test kills.
- Iter 3: `wf_947aaf7c-1b6` (U repair → delta review on `c7ccc2e..HEAD` ‖ F1 one guard test, no review round — a single test whose mutant leg is reported by the implementer and re-run by the lead).
- m60 decomposition DRAFTED (`m60-decomposition.md`; registered — R25.3 + bookkeep — only when m59 is DONE). Lead probes (`<scratchpad>/m60/`, m58 tree): (O) `x.map(lambda v: v+1)` and `x.map(lambda v: v*100)` COLLIDE into one node today and the second materializes as the first — far commoner than the findings' closure-factory case; identity travels in the `fn` PARAM (frozen toy backends hash `params["fn"]`) → per-Session first-seen ordinal on the derived name. (X) the foreign-Array shape is wider than `record_external`: `record_exchange` accepts, `record_join` has no check either (its refusal was an unrelated form error), and `s.materialize(y)` / `s.form(y)` / `compile_ir(s, y)` with `y` from another Session silently answer with `s`'s node of the same id (the notebook re-run hazard) → one guard across every Array-taking Session entry. (P) parquet chunks already carry record parameters (`ak.from_parquet`); only the FORM drops them. `ak.from_arrow_schema`, `ak.from_arrow(empty_table)` and `ak.metadata_from_parquet` all drop them too; the public route that keeps them = awkward's own reader over a zero-row file of the schema (form == eager on awkward-written files; == today's form on a pyarrow-written one). `awkward._connect` stays out (the repo's `_write_augmented` rule).
- m59 iter 2 (`wf_b2f640e8-8b6`): a543b6b `fix(m59): a leading ... must have an axis to absorb`; delta reviewer **APPROVE**, 0 design findings, 12 mutants: 10 killed, 2 measured equivalent. Implementer corrected the dispatch by measurement: awkward's depth is `layout.minmax_depth[0]`, not `ndim` (`ndim` is 1 for a record of jagged fields, and would refuse the frozen `[..., 0]` leg; the reviewer's M11 mutant witnesses it). Ships as written (spec-conformant over-refusals): `x[..., :]` at depth 1, `x[..., :, 0]` at depth 2. **graphed#33** opened, base `m58-projection-declarations`, tag `freeze-m59` pushed; CI watcher background `bnl81k7jy` (a543b6b, 90 min cap). m60 registers when that CI is green.
- graphed#33 CI: 32/32 pass on a543b6b → m59 = DONE pending the owner's merge. m60 registered: R25.3, `bookkeep --set-current m60`, meta commit 9468c5d (local; also carries the plan's m60-row + decision-5 edits). Branch `m60-integration-seams` off a543b6b. TEST_AUTHORING: `wf_369c2070-e8b`.
- U + F1 iter 3 (`wf_947aaf7c-1b6`).
  - U e37b22d: one `_GraphedTTreeSource.open_tree` (the only partition-open expression; `_write_partition` and `read_partition` both call it), two-leg MARK-executor identity test (each leg dies under its own mutant), object-path name test parametrized over `Events` / `dir/Events` / `dir/Events;1` + the fallback leg (the empty object path IS reachable, so the fallback was tested, not deleted), 4 dead `:doc:` roles fixed. Delta reviewer: all three closed, REJECT on 2 — (D1, docstring-only) the replacement sentence credited `graphed_partitions`/`read_graphed_partition` with running a recorded analysis; a call trace shows the drivers use the source's own `partitions`/`read_partition`; (D2) `file_tree[0][1] if file_tree else None` — the else arm is unreachable (`regularize_files` raises on empty) and coverage.py does not split ternary arms, so the 100 % gate never saw it. Exit notes: multi-file `__call__` concatenate arm untested; an unclosed handle in a test helper.
  - Lead cut all four directly (fbc79aa; ~10 shipping lines, no round): both docstrings name the drivers; the dead arm deleted; two-file loader test (dies under the forced single-file arm: 1 failed / 16 passed); `with` on the handle. Closing evidence run: 131 passed; the three name mutants still die (2 / 1 / 1 failed; control 17 passed); 100 % line+branch on both modules; docname instrument 0 unresolved on `_graphed.py`, `_graphed_write.py`, `_dask.py` control, synthetic `NOPE` flagged; hooks pass. **U review loop closed** (counts 3 → 3 → 2 → 0 open). Branch stays local until its upstream needs exist (#1720 merged + R).
  - F1 5e68a81: mixed-ndim Varied refusal test (kills `all`→`any`; implementer corrected the premise by measurement — the admitted shape does not segfault, it silently clusters the flat universe as ONE event); the jetdef test's sample now separates anti-kt R=0.4 / anti-kt R=0.6 / kt R=0.6 by VALUE. Lead check: 146 passed. **F1 closed** except its m60 follow-through (cross-session test, `register_internal("fastjet")`, public `graphed.expand`).
- Fork dev envs re-pinned: worktree moved `integ/graphed-m58` → `~/vibe-coding/integ/graphed-pin`, detached at m59 head a543b6b (Python-only milestone, the copied `.so` stays valid); both `graphed_dev.pth` files rewritten; on it U = 131 passed, F1 = 146 passed.
- Unit A1 started beside m60 (its only m60 need is the `register_internal("coffea")` provenance leg → follow-through, like F1's): coffea clone branch `graphed-mvp` off upstream master c917005e; today `mode="graphed"` → `ValueError: Invalid mode`. TEST AUTHORING: `wf_609d2a81-e64` (opus/xhigh; stand-in plugin under `<scratchpad>/a1/standin/`; files `tests/test_nanoevents_graphed.py`, `tests/test_nanoevents_graphed_routes.py`).
- m60 test authoring (`wf_369c2070-e8b`): `tests/frozen/frontend/m60/` (37 tests: session guard, callable identity, internal frames, public expand; helper libs `m60_lib/`, `m60_libx.py`) + `tests/frozen/awkward/m60/` (6: parquet form). Unimplemented tree: frontend 27 fail / 10 pass, awkward 3 fail / 3 pass; lead re-ran twice per dir → identical FAILED lists; 13 control mutations reported, each kills; all 43 pass under the author's stand-in plugin. Gaps accepted into the decomposition: `Session.serialized_ir` is an Array-taking reader the instrument finds (guarded, TypeError); X2's instrument keys on a `session` PARAMETER (`compile_ir` annotates its arrays `Any`). Softened: O4 pins `name=` in the three docstrings (Array.map has none today); V2's thread-safety = 8 concurrent registrars agree. P3 already holds today → regression guard on the new route (the zero-row scratch file must live outside the dataset paths). No frozen-vs-frozen conflict from exporting `expand` (m48's disposition walk keys on `Array` annotations; 112 m48 tests pass with it exported).
- Gate ok (suite untracked) → commit 720944b, tag `freeze-m60` (local). m60 IMPLEMENT→REVIEW: `wf_ba19c2ae-2b4`.
- A1 test authoring (`wf_609d2a81-e64`): coffea 86d3f89b — `tests/test_nanoevents_graphed.py` (454 lines) + `tests/test_nanoevents_graphed_routes.py` (237); 59 items. Today: 6 pass (exactly the controls) / 17 fail / 36 fixture errors (`Invalid mode graphed`), two runs identical; under the author's stand-in plugin (`<scratchpad>/a1/standin/a1_standin.py`) 59/59; 4 mutations, every control dies under one and survives the others. Measured for the implementer: driver backend carries the schema behavior, worker factory must not; `pickle.dumps(plan)` dies on vector lambdas held by `_map_schema_base.behavior` → `__reduce__` rebuilding from `schemaclass`; route (e) is `_DaskProperty` only; no_dispatch needs an explicit marker; cross-references at steps_per_file=2 EQUAL whole-file eager on both samples (no dask-oracle leg needed); route population NanoAOD 2292 / PFNano 2756 holder-name pairs, 0 unclassified; read list for (Muon.pt, num(Jet)) = `('Muon_pt','nJet','nMuon')`.
  - Lead decisions on its gaps: worker factory = `coffea.nanoevents._graphed:graphed_backend`; slot storage `_graphed_get`; every capability refusal is `NotImplementedError` with its pointer pinned by regex; the `metric=` callable refusal does NOT point at `graphed.vary` (wrong pointer for a metric) — re-pinned to a message naming the callable (lead edit of one regex before the commit; stand-in updated, 59/59).
- **m60 iter 1 (`wf_ba19c2ae-2b4`): TEST DISPUTE, correctly filed** (`.graphed/m60/disputes/m25-…md`; no commit). Implementation complete on the working tree (10 files +179/−23; one `Session._mine` guard, one `Session._fn_name` helper, `register_internal`, zero-row parquet form, `expand` export, docs; m60 frozen 37/37 + 6/6; 16/16 branch mutants killed; 100 % diff coverage from the frozen suites). Whole tree: exactly ONE red — frozen `preserve/m25::test_histogram_terminal_bundle_reproduces_bit_for_bit` calls `build_bundle(session=_record()[0], value=_record()[1])`: two Sessions; it passes today only because the two recordings are structurally identical (lead verified the call site). The new guard is right and the old test carries the bug it exists to catch. Proposed correction: take both halves from one `_record()` (3 lines). An earlier DONE milestone's frozen pin → **owner affirmation needed** (precedent: the 2026-09-07 owner-ruled re-freezes). Patch parked `<scratchpad>/m60/m60-impl-iter1.patch`.
- In flight: `wf_e07952a4-625` = A1 implementer → reviewer ‖ m60 early review of the working tree (the m25 red declared known; verifies the dispute's facts).
- m60 early review (in `wf_e07952a4-625`, working tree): dispute facts CONFIRMED (the m25 leg mixes sessions; whole tree has exactly that one red; histogram suite green). 32 mutants: 30 killed, 2 survivors. REJECT, 3 design findings, all in the two new helpers: (1) a bound method is a fresh object per access → the `id(fn)` memo never hits: `x.map(c.scale)` twice was ONE node at the freeze, TWO now; a loop grows the graph linearly (the §A.3 shape); (2) a declared `name="q"` is outside the derived-name space → a later un-named `q` silently takes the declared node and result; (3) `register_internal("lib.")` stores `"lib.."` and silently no-ops. Survivors: the ordinal counter saturating at 1 (no third-callable leg); the `'lambda'` default for a `__name__`-less callable.
  - Lead decisions: memo keyed on the callable ITSELF (measured: bound-method `==`/hash = `__self__` identity + `__func__`, a dict hits on a fresh access and misses another `self`; functions/partials by identity; unhashable instance → deliberate fallback); ONE per-Session set of every name handed out, declared or derived, a derived candidate advances until free; trailing dots stripped, empty prefix → ValueError. Dropped: consolidating the four pre-existing module-level session checks (`apply`, `apply_gufunc`, `aggregate_plan`, `join`) — they already refuse loudly and frozen suites pin their types; `var * ?unknown` vs `unknown` for an all-empty list field — an untyped column carries no values.
- m60 iter 2: `wf_78e8190c-130` (commit iter 1 as the two feat commits → `fix(m60)` → delta review; verdict ceiling APPROVE_PENDING_DISPUTE).
- A1 iter 1 (in `wf_e07952a4-625`): implementer green — coffea 7e97c75c (`_graphed.py` 120 lines = the one dispatcher + backend + worker factory `graphed_backend`; util.py slot/marker/refusal; factory.py arms + `_map_schema_base.__reduce__`; schemas flag) + 2890b4bd (docs section, `graphed` extra, CI env on 8 install lines); 59/59; coffea baseline in this venv is NON-ZERO (36 failed — `dask_histogram` absent) and byte-identical before/after. Reviewer (49 mutants) REJECT, 4 design findings: (1) the `.graphed` descriptor slot is DEAD — route (c) records the identical node route (f) records (same node id with the arm monkeypatched out; 5 slot mutants survive) → +47/−14 in util.py and touches in base.py/edm4hep.py bought nothing; (2) two dead guards in the dispatcher; (3) every new guard is pinned on its refusing side only (10 survivors: `maybe_map_partitions` admitting dask arrays / working without graphed, dunder guard, field-first, positional callable, `known_base_form`, the hoisted `_tree_to_open`, forwarded uproot options); (4) `__reduce__` on `_map_schema_base` also governs the dask and parquet mappings and its extras conditional is untested. Non-regression of the dask arm MEASURED by the reviewer (identical computed cross-reference hash base vs HEAD).
  - Lead decisions: plan decision 3 loses route (c) (slot deleted; coffea's descriptors and behavior files go back to upstream bytes); the route test amended by the lead (884364f1: slot test removed, `_apply_global_index` must classify as the generic method route — 3 legs red at HEAD until the deletion); field-first stays and its oracle is the DASK arm (lead-measured: `dask_awkward.Array.__getattr__` tests `attr not in fields` first; eager awkward is behavior-first); `__reduce__` moves to `_map_schema_uproot`, extras passed unconditionally; exit items done now (`__slots__ = ()`, ImportError naming the install line like `_import_dask_awkward`).
- A1 iter 2: `wf_6d31ca0a-e24` (repair → delta review, whole-artifact pass only on a clean delta).
- m60 iter 2 (`wf_78e8190c-130`): 58cea95 + 5c08b15 (iteration 1, byte-identical to the parked patch — reviewer-verified) + 8418070 `fix(m60)`; whole tree = exactly the one known m25 red; 10/10 delta mutants killed. Delta reviewer: declared-name reservation and the `'lambda'` default CLOSED; REJECT on 2, both produced by the lead's own loosely-classed repair instructions: (1) "key the memo on the callable" = `==`/`hash` lookup → two different callables their class calls equal (frozen dataclass with `field(compare=False)`; a plain `__eq__` on a key field) share a node and the second returns the first's value (`got=(4.0, 4.0) truth=(4.0, 200.0)`); (2) `register_internal` still registers `" "`, `" mylib"`, `"my lib"`, `"123"`, `"-"`.
  - Shape (stated once): a repair instruction that names a class by an example admits the example's neighbours; the instruction must state the class boundary and ship a member at each end. Lead-measured rule for (1), 13 members all as wanted: a callable with a non-None `__self__` = owner IDENTITY + `__func__` (or `__name__` for builtin methods / method-wrappers), anything else = its own identity, never `==`. For (2): every dotted component `isidentifier()` after `rstrip(".")`, replacing `if not prefix`.
- m60 iter 3: `wf_45b07997-7d1` (one `fix(m60)` → delta review; whole-artifact pass only on a clean delta). Round counts 3 → 2.
- m60 iter 3 (`wf_45b07997-7d1`): 76500f8. `register_internal` CLOSED (14 refused / 11 admitted members by the reviewer's probe). REJECT on the identity key again, 2 findings: (1) the owner+`__name__` key buckets every `functools.partialmethod` on one owner into `(id(owner), "lambda")` (a `partial` with a copied `__self__`, no `__func__`, no `__name__`) → `node ids 1 1 SHARED … 4.0 4.0`, truth `4.0 200.0` — a regression vs the previous key; (2) `key = (owner, func)` (owner `==`/hash) survives the whole m60 suite.
  - **Non-convergence on one 5-line unit** (design findings 3 → 2 → 2; the same key repaired three times, each widening admitting a neighbour). Unit shrunk to the key alone and the rule restated as Python's own definition instead of a constructed decomposition: merge ONLY a genuine `types.MethodType` → `(id(__self__), id(__func__))` (its call IS `__func__(__self__, …)`); everything else `id(fn)`. Lost CSE for re-accessed builtin methods / method-wrappers / partials is the accepted ceiling (never wrong). Lead-measured: correctionlib's pybind11 `evaluate` IS a `types.MethodType` → the HEP case keeps CSE; two partialmethods get distinct keys. A second non-convergent round on this unit surfaces to the owner.
- m60 iter 4: `wf_5513302e-665` (the key + a member at each end → unit review alone, whole-artifact pass if clean).
- A1 iter 2 (`wf_6d31ca0a-e24`): implementer worked in a scratch clone (`/private/tmp/a1i2`) — lead fast-forwarded the canonical clone to b2bc09b0 (8ff3a6ac slot deleted: `methods/base.py` + `methods/edm4hep.py` byte-identical to upstream again, the `_DaskProperty` no_dispatch marker deleted too as measured dead; 62e5f3c0 `__reduce__` on `_map_schema_uproot` only; 0c7b8159 ImportError naming the install; b2bc09b0 `tests/test_nanoevents_graphed_guards.py`, 15 mutants 14 killed + the 1 deliberate survivor that justified a deletion). Delta reviewer: all 6 previous items CLOSED, 23 mutants killed; its whole-artifact pass found ONE design finding — the CI `GRAPHED_EXTRA` conditional re-encodes six times the floor the extra's own marker already states (measured: on CPython 3.10 `uv pip install '.[graphed]'` resolves the extra empty, exit 0; 3.11 control demands it). Lead cut it directly: 12e94b75 (`,graphed` on all 8 install lines, original quoting; one docstring phrase). Canonical clone: 73 passed. **A1 review loop closed** (4 → 1 → 0). Remaining for A1: the m60 follow-through (`register_internal("coffea")` + provenance test), floors at R / the uproot release, then the upstream PR.
- Side findings from A1's implementer, sized and decided:
  - RNTuple NanoAOD under graphed mode failed at RECORD time (`KeyError '__doc__'`): `uproot.graphed` (#1720's code) took an RNTuple's base form from `to_akform`, dropping `ak_add_doc`, while `_get_ttree_form` already handles `HasFields`. Root-cause deletion in the U branch: bfcd7c2 + a TTree/RNTuple parametrized base-form test (RNTuple leg dies on the old code); U now 133 passed, 100 % line+branch; record-time form == dask meta on `nano_dy_rntuple.root`.
  - RNTuple READ then fails `'RField' object has no attribute 'title'` — measured IDENTICAL in coffea's own dask arm (`Muon.pt.compute()`), eager works: coffea passes `ak_add_doc={"__doc__": "title"}` to `RNTuple.arrays`. Dropped: a pre-existing coffea↔uproot defect of the sibling arm that graphed mode merely mirrors; fixing it here would change dask behaviour in a graphed PR.
  - dask mode from an already-open `ReadOnlyDirectory` raises `KeyError 'nMuon'` on compute at upstream c917005e — pre-existing, dropped for the same reason (the new dask leg asserts the form, which is what the hoisted `_tree_to_open` decides).
  - PHYSLITE: graphed records 1244 fields vs dask meta 1201 → plan §6 "other schemas" phase (probe against data before admitting any).
- m60 iter 4 (`wf_5513302e-665`): implementer f5ad10b — key = `(id(__self__), id(__func__))` for a MethodType, `id(fn)` otherwise; 9 members both ends, 4 key mutants killed. Unit reviewer REJECT with ONE design finding (counts 3 → 2 → 2 → 1, converging): the predicate `isinstance(fn, types.MethodType)` consults the callable-controlled `__class__`; two transparent proxies over one method merged (`node ids 1 1`, `4.0 4.0` vs truth `4.0 200.0`); `types.MethodType` is not subclassable, so `type(fn) is` excludes nothing genuine; the narrowing mutant SURVIVED the suite. The rule stood — the predicate was wider than it. Lead cut it directly: 1e4354a (`type(fn) is types.MethodType`, the proxy closing test — measured failing on `isinstance` with `assert 1 != 1`, 73 passed after; the reviewer's second test dropped: its merge half duplicates `test_two_method_objects_fabricated_from_one_pair_are_one_node`, and a subclassable MethodType would land at the accepted lost-CSE ceiling, never a wrong answer; two wording fixes — docstring partial clause, attempts.md "correctionlib keeps CSE *when the correction object is held*"). Gate ok. Dispatched `wf_9a8d9df3-8c4`: delta review + the owed whole-artifact pass over `freeze-m60..HEAD`.
- m60 follow-through in the forks, done against `integ/graphed-pin` re-pinned to 1e4354a (the X/V/E surfaces closed in earlier rounds; only the identity key was still in review). Each premise measured first (provenance landed in the library's frame), each test run both ways (as shipped / registration removed → the library's file where the test's file is expected):
  - uproot `graphed-form-mapping` 6bdc948: `register_internal("uproot")` inside `uproot.extras.graphed()` — the ONE route by which uproot imports graphed; a graphed too old to have the hook is reported like a missing one (`AttributeError` joins the except, message names 0.0.3). 135 passed, `_graphed.py` 100 % line+branch, changed `extras.py` lines all hit. TRAP re-met: `prek -a` rewrote `pyproject.toml` (local toml-sort 0.25) — reverted; run hooks with `--files`.
  - fastjet `graphed-arm` 7751717: `register_internal("fastjet")` at the arm's module top, `expand` from `graphed` (public), cross-session refusal test (control: `Session._mine` no-op'd → `DID NOT RAISE`), provenance test. 148 passed.
  - coffea `graphed-mvp` (HEAD after 12e94b75): `register_internal("coffea")` at `nanoevents/_graphed.py` module top (driver route + worker factory both import it); one test over source / field / behavior method. 74 passed. The `source` leg needs uproot's 6bdc948 too — same uproot-release dependency A1 already has.
- m25 dispute, correction measured (scratch copy `<scratchpad>/m60/m25fix`, frozen tree untouched): unchanged copy → the known red `TypeError: … recorded in a different Session` (control); with the 3-line correction → 4 passed. To add to the dispute file once `wf_9a8d9df3-8c4` is off the tree. Still needs the OWNER's re-freeze affirmation.
- m60 iter 5 review (`wf_9a8d9df3-8c4`): delta on 1e4354a — ZERO design findings; the identity key is CLOSED (the reviewer could not build a wrongly-merging member whose type IS `types.MethodType`: `__self__`/`__func__` read-only, type unsubclassable, `__class__` unassignable; an owner mutated between records merges to the same answer the forced-unmerged control gives). Whole-artifact pass RUN: 23 mutants, 22 killed, 1 equivalent; frozen untouched (live control); gates green but the known m25 red; histogram 214 passed. ONE design finding, in prose: design.rst called `expand` "the one every built-in verb uses" (false — tuple-returning / metadata verbs map differently; `expand` over a tuple verb answers one `Varied` of tuples). Lead cut it (graphed HEAD after 1e4354a, `docs(m60): …`): sentence scoped to a one-array verb, anchored on the measured `graphed.apply` → `expand` call; private `expand_tuple` not advertised; two exit-round docstring wordings; dispute file now carries the measured correction. Reviewer's proposed test dropped (pins a misuse's answer shape; the frozen `test_expand_stays_outside_…` already pins that expand is not the whole surface). The remaining delta is prose-only → no further round now; the recorded APPROVE comes from ONE final delta round after the owner's m25 re-freeze (it must cover that delta anyway, and APPROVE is impossible while the m25 leg is red). Counts on m60: 3 → 2 → 2 → 1 → 1(prose) → closed pending that round. `integ/graphed-pin` moved to the new head.
- **OWNER RULING 2026-09-19: "refreeze allowed"** (m25 dispute). Applied verbatim (diffed identical to the scratch-verified correction): graphed 4911bc5 `test(m25): re-freeze one disputed leg on the owner's affirmation`, annotated tag `freeze-m25-fixup` (convention: `freeze-m57-fixup`). Evidence: m25 dir 4 passed; `./scripts/run-tests.sh` EXIT=0, 0 FAILED/ERROR over 81 progress lines (control: the prior log carries the 1 known red); gate WITHOUT the sanction FAILs `frozen-modified:…m25…` (control), with `--allow-refreeze tests/frozen/preserve/m25` → `REFREEZE:` advisory, ok. Frozen delta since `freeze-m60` = that one file. Dispute marked RESOLVED; attempts.md iteration 7. Final review round `wf_0cb2b217-67f` (delta 1e4354a..4911bc5: prose + re-freeze; must show the corrected leg can still fail in its guarded direction) → records the verdict. PR body drafted at `<scratchpad>/m60/pr-body.md`. Pin → 4911bc5.
- m60 FINAL REVIEW `wf_0cb2b217-67f`: **APPROVE**, zero design findings; re-frozen leg proven to still red on a differing second build (fill weight / bin count), `_record()` runs twice; AST scan of `tests/frozen` for two Session-minting calls in one call expression: 1 hit at `freeze-m60` (the m25 leg), 0 at HEAD; all gates green (whole tree rc=0). Two wording notes folded in → graphed 9db30fb. Pushed `m60-integration-seams` + tags `freeze-m60`, `freeze-m25-fixup`; **graphed#34** opened, stacked on #33. CI watcher `bty4rgy3w` (headSha match, self-expiring). Project memory written: `integration-arc-coffea-fastjet-conda`.
- graphed#34 CI: **32/32 SUCCESS** on 9db30fb (incl. `docs (sphinx -W)`, both 3.14t jobs, wheels) → m60 Definition of Done fully ticked. Meta bookkeeping 6dfc0ec (state note gains m54–m57 + the m58–m60 arc; this journal became tracked in that commit — scanned: no emails/secrets, sibling worklogs are tracked too).
- R (release request) made concrete: versions are static literals, so 0.0.3 needs a prep PR per repo (precedent graphed#30). **graphed#35** `chore(release): 0.0.3`, branch `release-0.0.3` stacked on m60 (11d0e7c): 12 files bumped (instrument: `git grep "0\.0\.2"` outside changelog/MIGRATION → none; control 12 files carry 0.0.3), `cargo metadata --locked` ok, changelog 0.0.3 section (its before/after lambda claim re-measured: `1 2`/`4 200` now vs `1 1`/`4 4` under the v0.0.2 rule, which `git show v0.0.2:python/graphed/array.py` confirms was `name or fn.__name__`), docs python blocks re-run (the same 4 isolated-fragment NameErrors on the v0.0.2 text and here). `uv lock --check` stale on the untouched m60 head too and no workflow consumes `uv.lock` → version line only, as #30 did; full re-lock dropped (no consumer). CI watcher `brnuqhmz2`. Sibling release PRs (histogram, executors) wait for graphed's release commit on main (their CI installs graphed from a git sha carrying the bump) and their LICENSE PRs #16.
- graphed#35 (release prep 0.0.3) CI: **32/32 SUCCESS** on 11d0e7c, PR CLEAN (the `sphinx -W` job passed the new changelog section). The arc is now blocked on the owner only: merge #31, then #32 → #33 → #34 → #35, cut `v0.0.3`. Next for the lead after that: histogram + executors 0.0.3 prep PRs (graphed git sha carrying the bump; their LICENSE #16), conda bump on staged-recipes#34887, then the three upstream PRs as their needs arrive.
- **OWNER AUTHORIZATIONS 2026-09-19:** (1) "all license files … are now enqueued for merge. Once those are all in you may proceed with the remaining merge-order" → lead lands #32 → #33 → #34 → #35 through the merge queue. (2) "You may also cut the graphed (graphed-executors, and graphed-histogram) v0.0.3 release once it is appropriate to do so." → lead cuts `v0.0.3` on all three, order graphed → histogram → executors. Queue on graphed main: method SQUASH, `delete_branch_on_merge=false`. Stack-landing procedure (no force-push): after the parent squash-lands, `git merge -X ours origin/main` into the child branch, verify `git diff <old tip> HEAD` = only what main newly brought, `gh pr edit --base main`, wait for `ci required`, enqueue (`session-12dd48ff-handoff/tools/enqueue_one.sh`). Watcher `b938qgnnf` waits for the four LICENSE PRs.
- Landing the stack (2026-09-19): LICENSE PRs graphed#31 (c8f58e2), executors#16, histogram#16 MERGED; orchestrator#1 green but that repo has NO merge queue / auto-merge so it was never enqueued — left for the owner, blocks nothing. #32 landed as 653e404. Rehearsal on a simulated post-m58 main: each child branch merges clean with `-X ours` and its tree changes by exactly `LICENSE` (+21). #33: merged main into `m59-awkward-idiom-parity` (d7a0f8b; tree delta vs old tip = LICENSE only; m59 frozen dirs unchanged since `freeze-m59`), pushed fast-forward, retargeted via REST (`gh pr edit --base` dies on the Projects-classic GraphQL deprecation → `gh api -X PATCH repos/…/pulls/N -f base=main`), lander `b7f1kzxou` (`<scratchpad>/m60/land_one.sh`: enqueue on `ci required` SUCCESS, wait MERGED, stop on bounce). Release notes for graphed v0.0.3 converted from the changelog: `<scratchpad>/m60/release-notes-graphed-0.0.3.md`.
- CORRECTION (lead's own mislabel): `graphed-orchestrator#1` was never a LICENSE PR — it is `fix(precommit): a staged-new frozen suite is new, not modified` (from the m58 freeze). Owner 2026-09-19: "merges into orchestrator are allowed" → squash-merged cef1293. Measured licence state of the org: graphed / executors / histogram carry LICENSE; orchestrator, corpus (`graphed-corpus-mvp`) and the meta repo did not, though orchestrator + corpus declare `license = "MIT"`. Owner: "license file for meta repo as well. MIT." → meta 2fd6050; **orchestrator#2**, **corpus-mvp#3** opened (same text as graphed#31; sdist before/after: `License-File: LICENSE` appears, control leg = the before build). Watcher `bdnvgl6z3` squash-merges orchestrator#2 on green; corpus#3's merge is left to the owner (not authorized).
- **Owner 2026-09-19: "I have stacked PRs 33-35 in the github UI so they can simply all be merged as one now, with separate review."** Measured: GitHub's native stacked-PRs feature (public preview 2026-07-30) — timeline `added_to_stack` on #33/#34/#35, `GET repos/graphed-org/graphed/stacks/36` (base main, positions 1–3). Docs read (docs.github.com: merging-stacked-pull-requests, troubleshooting-stacked-pull-requests, rest/pulls/pulls#merge-a-pull-request-asynchronously): a stack merges bottom-up and ATOMICALLY through `PUT /repos/{o}/{r}/pulls/{n}/merge-async` (`X-GitHub-Api-Version: 2026-03-10`; body `merge_method`, `merge_action: merge_queue`, `sha`; poll `GET …/merge-async/{uuid}`); the legacy merge endpoints and the `enqueuePullRequest` mutation CANNOT merge a stack; the stack must be LINEAR (fix = `gh stack rebase`+`push` or the "Rebase stack" button; local gh 2.69.0 has no `gh stack`). My earlier `merge -X ours` of main into m59 (d7a0f8b) broke linearity → superseded: stopped the legacy lander, linearised with plain `git rebase --onto` per branch (m59 1f20586 ← m60 9c29bfc ← release 6246b60 on main 653e404). Verified before pushing: each new tree == its reviewed old tip + `LICENSE` only; commit counts 4/14/15 preserved; 0 merge commits; ancestry chain ok. Force-pushed with per-branch leases; local backups `backup/<branch>-pre-stack-rebase`; freeze tags still name the pre-rebase commits (tree-based checks unaffected). CI waiter `bylwjfwvv`; the merge-async call on #35 is issued by the lead after ALL-GREEN.
- Licences done across the org: orchestrator#2 MERGED 3323e53 (20/20 checks; my first watcher false-alarmed — pending checks report `conclusion: ""`, not null; use `gh pr checks --json state`), corpus-mvp#3 MERGED c5997b3 on the owner's "it's ok to merge into corpus" (19/19), meta 2fd6050. All six public repos now carry the MIT text. Stack #36: a first `merge-async` on #35 was refused 422 when `merge_method` was passed ("not supported with the merge_queue merge action" — the queue owns the method), then accepted `pending` without it and FAILED in the background: `Required status check "ci required" is expected` → the PR-level check gates the stack exactly as it gates a single PR ([[merge-queue-enqueue-precondition]]); reissue after `bylwjfwvv` reports ALL-GREEN. UI name for the same action: "Enqueue stack".
- Stack #36 ENQUEUED as one unit after 32/32 green on all three rebased heads: `PUT pulls/35/merge-async` (`merge_action=merge_queue`, `sha`, no `merge_method`) → `pending` → poll → `enqueued`; queue `#33@1,#34@2,#35@3`. Watcher `blzndv3ov`.
- Pre-release compat gate (lead's own finding): both siblings' only commit since `v0.0.2` is LICENSE, their runtime floor is `graphed>=0.0.2`, and executors' CI pins graphed at the 0.0.2 sha — so executors had never met m58–m60 while graphed 0.0.3 reaches its users at the tag. Measured: executors 0.0.2 source (`graphed-exec-local` @ 3afd1cf) `pytest tests/frozen tests/extra/m41` against the 0.0.3 tree → EXIT=0, 411 tests / 6 progress lines, 0 F/E (first attempt void: 4 collection errors, `graphed_corpus` absent from the m52 venv → `PYTHONPATH=graphed-corpus/src`; caught by the zero-progress-lines control). NOT run locally: 43 dask + 25 parsl tests (extras not installed). Histogram: 214 passed vs the m60 head (m60 reviewer). DECISION: siblings' CI needs graphed's release COMMIT, not the tag → after the stack lands open both sibling prep PRs pinned to that sha, let their CI (incl. dask/parsl jobs) go green, THEN cut graphed `v0.0.3`.
- Stack #36 outcome under the SQUASH queue (measured): #33 MERGED dfe4936 (main's tree == the reviewed m59 tip's tree; control differs); at that same second `github-merge-queue[bot]` `removed_from_merge_queue` #34 and #35 (no reason recorded, their queue runs still in progress). GitHub did NOT auto-rebase/retarget #34 (base stayed the m59 BRANCH @1f20586; `stack.base` read `main@1f20586` — it models trunk as containing #33's original commits, which a squash replaces), and `PATCH pulls/34 base=main` → 422 "Cannot change the base branch because the pull request is part of a stack". Stacks REST surface is list/create/get/add/unstack only (docs.github.com/en/rest/pulls/stacks) — no restack/retarget. `gh stack sync` is documented as fetch + ff trunk + rebase remaining + push, i.e. what the lead did by hand (m60 d80fd9a, release 610334a on main dfe4936; trees byte-identical to the reviewed tips, control differs; 10/11 commits, 0 merges). DECISION: every stack-preserving lever (delete the merged m59 branch; force-move it to main) rides undocumented preview behaviour on release PRs, so fall back to the route proven on #32 — `POST stacks/36/unstack` (200; reversible via `POST /stacks`), `PATCH #34 base=main` (30 changed files == local diff; `blocked` on the required check, not `dirty`), lander `bfixsjcdn`. After #34 lands: rebase release onto main, retarget #35, land. TRAP for the memory: a native GitHub stack + a SQUASH merge queue lands one layer per cycle at best; with `delete_branch_on_merge=false` nothing retargets the next layer.
- **CORRECTION of the previous entry's diagnosis.** Owner: "See if you can force-restart CI checks on 34 - something seems stuck." Measured: (1) NO workflow run existed for #34's head d80fd9a — it was pushed while #34 still targeted the m59 branch and was `dirty`, and GitHub starts no `pull_request` workflow for a conflicted PR; the later base change is an `edited` event, outside CI's default types → fixed by close/reopen (`reopened` fires; no history change), runs queued 16:54:46Z. (2) The #34 merge GROUP run had **failed**: one job, `test windows-latest py3.12`, `tests/frozen/core/m4/test_benchmark.py::test_each_size_reduces_within_budget — AssertionError: n=4000 took 1.269s` — a wall-clock budget flake on a shared Windows runner (m60 is Python-only; the same tree passed that job on three PR runs, and the #35 group, which contains all of #34, passed it). THAT ejected #34, and the documented rule "an ejected PR takes everything above it" ejected #35. My "a squash queue lands a stack one layer per cycle" was an unmeasured inference drawn while the run still read `in_progress` — wrong: the queue chains the groups itself (pr-33 → pr-34-on-dfe4936 → pr-35). The unstack + rebase were therefore unnecessary though harmless (identical trees). Restored the owner's setup: `POST /stacks {"pull_requests":[34,35]}` → stack #37 (#34 → main, #35 → m60 branch; #35 32/32 green on 610334a). Driver `bo4ym5mg9`: #34 `ci required` SUCCESS → `merge-async` on #35 → both MERGED; on any failure it prints the failing JOB names. Standing fact: `m4/test_benchmark.py` wall-clock budget can flake on windows-latest → `gh run rerun --failed`, not a code change.
- Stack #37 LANDED TOGETHER (2026-09-19): `merge-async` on #35 → `enqueued`; #34 MERGED 5c283e5, #35 MERGED 559ed8d (no flake this time). graphed `main` = 559ed8d3c84e249e9898b6159b7860ef2ecf5530, tree == the reviewed release tip 610334a (control vs d80fd9a differs), `version = "0.0.3"`. The whole arc's graphed side is on main: #31 c8f58e2, #32 653e404, #33 dfe4936, #34 5c283e5, #35 559ed8d.
- Sibling release-prep PRs opened against that commit: **graphed-histogram#17**, **graphed-executors#17** (scratch worktrees `<scratchpad>/rel/{histogram,executors}`, branch `release-0.0.3`): version literals + `docs/conf.py` → 0.0.3, changelog "Packaging" section, CI graphed pin 6464a3a → 559ed8d (+ histogram `docs/requirements.txt`); runtime floors stay `graphed>=0.0.2` (nothing needs 0.0.3; instrument: `git grep 0\.0\.2` outside the changelog → floors only). Gates ok in both. Watcher `bep883ksk`: both PRs' checks + graphed main's runs on 559ed8d → ALL-GREEN is the precondition for the graphed `v0.0.3` tag (executors' dask/parsl jobs are the compat legs not runnable locally).
